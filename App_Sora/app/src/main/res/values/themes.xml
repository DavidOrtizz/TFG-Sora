<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Sora" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/fondo_oscuro</item>
    </style>

    <style name="Theme.Sora" parent="Base.Theme.Sora" />

    <style name="BottomNavigation">
        <item name="android:background">@color/fondo_menu</item>
        <item name="itemIconTint">@color/white</item>
        <item name="itemTextColor">@color/white</item>
    </style>
</resources>